services:
  frontend:
    image: node:20
    container_name: froodo-frontend
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev"